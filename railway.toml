# Railway Deployment Configuration
# This file configures Railway.app deployment

[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "cd server/server && npm start"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[env]
  NODE_ENV = "production"
  PORT = "4000"