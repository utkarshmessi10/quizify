{
  "version": 2,
  "builds": [
    {
      "src": "server/server/app.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["server/server/data/**", "server/server/public/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/server/app.js"
    },
    {
      "src": "/static/(.*)",
      "dest": "server/server/public/static/$1"
    },
    {
      "src": "/(.*\\.(js|css|png|jpg|jpeg|gif|ico|svg))",
      "dest": "server/server/public/$1"
    },
    {
      "src": "/(.*)",
      "dest": "server/server/app.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}